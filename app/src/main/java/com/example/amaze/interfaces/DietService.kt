package com.example.amaze.interfaces

import com.example.amaze.network.DietResult
import com.example.amaze.utils.SecureStorageServices
import retrofit2.Call
import retrofit2.http.GET
import retrofit2.http.Header

interface DietService {
//    @GET("diets")
//    fun getUserDiets(
//        @Header("Authorization") auth : String = "Bearer ${SecureStorageServices.authJwtToken}") : Call<ArrayList<DietResult>>
//    )
}